#!/usr/bin/env python3
"""
AETHER CLI launcher.

This wrapper should behave like the installed `aether` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from aether_cli.main import main
    main()
